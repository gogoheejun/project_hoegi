<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DaeDan" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green1</item>
        <item name="colorPrimaryVariant">@color/green2</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorSecondaryVariant">@color/green3</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomSheetTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottomsheetshape</item>
    </style>


    <style name="ToolbarStyle">
        <item name="android:background">@color/green2</item><!--네임스페이스가 android면 버전영향 안받는애들임,,,
        아래에 titleTextColor는 버전 21이상부터만 가능함...우리 미니멈이 19잖어 그런건 app:임(원래 activty_main)에 app:titleTextColor라고 해주는거를 여기 테마에다가 써주는거임-->
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Toolbar">
        <item name="background">@color/green3</item>
        <item name="colorControlNormal">@color/green3</item>
    </style>

<!-- =============-->



</resources>